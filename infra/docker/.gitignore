# Environment files with secrets
.env
*.env.local

# Data directories (if using local bind mounts)
data/

# Backup files
backups/
*.sql
*.rdb

# Allow init scripts (override *.sql above)
!init-scripts/
!init-scripts/**

# Log files
*.log

# SSL certificates (if stored locally)
*.pem
*.crt
*.key
